import java.util.Scanner;

public class monamonamona
{
    public static void main(String[] args)
    {
        Scanner sc = new Scanner(System.in);
        String[] names = sc.nextLine().split(" ");
        System.out.printf("%s %s %s", names[0],names[0],names[0]);
    }
}
