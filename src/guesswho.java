import java.util.*;

public class guesswho
{
    public static void main(String[] args)
    {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt(), m = sc.nextInt(), q = sc.nextInt();
        List<String> characters = new ArrayList<>();

        for (int i = 0; i < n; i++)
        {
            characters.add(sc.next());
        }

        sc.nextLine();


    }
}
